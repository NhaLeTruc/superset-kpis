# Python dependencies for GoodNote Analytics Platform
# Install with: pip install -r requirements.txt

# Testing
pytest>=8.3.5
pytest-cov>=5.0.0
chispa>=0.9.4  # PySpark testing utilities

# PySpark (requires Java 8+/11)
# Note: PySpark installation may require Docker environment
# See docs/SETUP_GUIDE.md for Docker-based setup
pyspark>=3.5.0
py4j>=0.10.9.7

# Database connectivity
psycopg2-binary>=2.9.9  # PostgreSQL adapter

# Data processing utilities
pandas>=2.0.3
numpy>=1.24.4

# Utilities
python-dotenv>=1.0.0  # Environment variable management

# =============================================================================
# Development & Code Quality
# =============================================================================
ruff>=0.9.0           # Linting, formatting, import sorting
mypy>=1.14.0          # Static type checking
bandit>=1.7.0         # Security analysis (1.7.x for Python 3.8 compatibility)
pre-commit>=3.5.0     # Pre-commit hook framework (3.x for Python 3.8 compatibility)
